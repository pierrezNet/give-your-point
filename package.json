{
  "name": "give-your-point",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run build && wrangler dev --persist-to=./db_data",
    "deploy": "wrangler pages deploy dist",
    "build": "mkdir -p dist && cp -r public/* dist/ || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260128.0",
    "wrangler": "^4.62.0"
  }
}
